<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    <title>Document</title>
  </head>
  <body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
      <div class="w-full flex justify-end">
        <a
          class="bg-red-500 rounded-md px-3 py-2 text-sm mb-5 inline-block"
          href="/logout"
          >Logout</a
        >
      </div>
      <h5 class="mb-4">Edit your post.</h5>
      <form action="/update/<%= post._id %>" method="post">
        <textarea
          name="content"
          placeholder="What's on your mind ?"
          class="p-3 block outline-none mb-2 w-1/3 resize-none bg-transparent border border-zinc-500 rounded-md"
        >
        <%= post.content %>
        </textarea>
        <input
          class="px-5 py-2 w-40 block bg-yellow-500 rounded-md text-black"
          type="submit"
          value="Update Post"
        />
      </form>
    </div>
  </body>
</html>
